# Install as /etc/sudoers.d/owncord-deploy (root:root, mode 0440).
# Allows the gitlab-runner CI user to invoke the deploy script without password.

gitlab-runner ALL=(root) NOPASSWD: /usr/local/bin/owncord-deploy.sh
Defaults!/usr/local/bin/owncord-deploy.sh !requiretty
